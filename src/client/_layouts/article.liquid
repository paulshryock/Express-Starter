---
layout: constrain-pad
---
<article>
  <header>
    <h2>{{ article.title }}</h2>
    <p>By {{ article.author }} | Last updated: {{ article.date | date: "%m/%d/%Y" }} | Status: {{ article.status }}</p>
  </header>
  {% if article.content %}
  <div>
    {{ article.content }}
  </div>
  {% endif %}
</article>